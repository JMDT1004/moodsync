<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MoodSync</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="css/style.css">

</head>

<body>
  <header class="bg-gradient-to-r from-purple-400 via-green-400 to-red-400">
    <div class="text-slate-50 text-5xl text-center">MoodSync</div>
    <nav class="text-center font-medium">
      <a href="/" class="{{#if isHome}}hidden{{/if}}">Home</a>
      {{#if email}}
      <a href="/logout">Log Out</a>
      {{else}}
      {{#if isLoggedIn}}
      <a href="/mood">Mood</a>
      {{else}}
      <a href="/login" class="{{#if isLogin}}hidden{{/if}}">Login</a>
      <a href="/register" class="{{#if isRegister}}hidden{{/if}}">Register</a>
      {{/if}}
      {{/if}}
      <p class="{{#if isHome}}hidden{{/if}}{{#if isLogin}} hidden{{/if}}{{#if isRegister}} hidden{{/if}}">Welcome,
        {{email}}</p>

    </nav>
  </header>
  {{{body}}}

</body>

</html>